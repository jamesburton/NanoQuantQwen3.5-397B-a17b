---
phase: 04-phi-moe-support
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - scripts/eval_ppl.py
  - scripts/reconstruct_model.py
  - scripts/run_eval.py
  - results/Phi-tiny-MoE-instruct/metrics.json
  - results/SUMMARY.md
autonomous: false
requirements:
  - PHI-02
  - PHI-03

must_haves:
  truths:
    - "Phi-tiny-MoE-instruct quantizes end-to-end without crashing on a 12GB consumer GPU"
    - "Quantized checkpoint is produced and loadable"
    - "WikiText-2 FP16 baseline perplexity is measured for Phi-tiny-MoE-instruct"
    - "WikiText-2 quantized perplexity is measured for Phi-tiny-MoE-instruct"
    - "Memory footprint (model size before/after, peak VRAM) is reported"
  artifacts:
    - path: "results/Phi-tiny-MoE-instruct/metrics.json"
      provides: "FP16 and quantized PPL + memory metrics"
      contains: "perplexity"
    - path: "results/SUMMARY.md"
      provides: "Updated summary table with Phi-tiny-MoE results"
      contains: "Phi-tiny-MoE"
  key_links:
    - from: "scripts/run_stage1.py"
      to: "output/Phi-tiny-MoE-instruct/"
      via: "Stage 1 quantization produces checkpoint"
      pattern: "checkpoint"
    - from: "scripts/run_eval.py"
      to: "results/Phi-tiny-MoE-instruct/metrics.json"
      via: "Eval pipeline measures and stores PPL"
      pattern: "metrics"
---

<objective>
Run Phi-tiny-MoE-instruct end-to-end through the NanoQuant pipeline and measure WikiText-2 perplexity.

Purpose: PHI-02 requires the model to quantize on a 12GB consumer GPU producing a valid checkpoint. PHI-03 requires WikiText-2 PPL for both FP16 baseline and quantized model. This plan uses the existing pipeline scripts with the SlimMoE model (which uses separate nn.Linear experts via trust_remote_code=True, handled by the existing nn.Linear path).

Output: Quantized checkpoint + metrics.json with PPL and memory numbers.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-phi-moe-support/04-RESEARCH.md
@nanoquant/moe.py
@scripts/run_stage1.py
@scripts/run_eval.py
@scripts/eval_ppl.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke test — quantize first 2 blocks of Phi-tiny-MoE-instruct</name>
  <files></files>
  <action>
Run a quick smoke test to verify the pipeline handles Phi-tiny-MoE-instruct without crashing:

```bash
python scripts/run_stage1.py \
  --model microsoft/Phi-tiny-MoE-instruct \
  --output output/phi-tiny-moe-smoke \
  --max-blocks 2 \
  --n-calibration 4 \
  --admm-iters 10 \
  --tpre 0 --tpost 0
```

If it fails, diagnose and fix the issue. Common issues per research:
- `_call_block` may need the `position_ids` fallback path for PhiMoEDecoderLayer (should work automatically)
- Router gate at `block_sparse_moe.gate` must be skipped by `is_router()` (already handled)
- Expert nn.Linear layers (w1, w2, w3) should be enumerated by the existing nn.Linear traversal

If the smoke test passes, proceed to the full run. If it fails, fix any compatibility issues in the pipeline code before proceeding.
  </action>
  <verify>
Smoke test completes without error. Output directory `output/phi-tiny-moe-smoke/` contains checkpoint files.
  </verify>
  <done>
Phi-tiny-MoE-instruct passes smoke test — first 2 blocks quantize successfully and produce a loadable checkpoint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full quantization of Phi-tiny-MoE-instruct</name>
  <files></files>
  <action>
Run full Stage 1 quantization:

```bash
python scripts/run_stage1.py \
  --model microsoft/Phi-tiny-MoE-instruct \
  --output output/Phi-tiny-MoE-instruct
```

Use default parameters (the script handles calibration, ADMM iterations, and checkpointing). Monitor for OOM — the model is 3.8B total with 1.1B active, well within 12GB VRAM budget.

If OOM occurs, reduce calibration samples or batch size. The model's small size (3.8B params) should fit comfortably.
  </action>
  <verify>
Full quantization completes. `output/Phi-tiny-MoE-instruct/` contains checkpoint safetensors + manifest JSON.
  </verify>
  <done>
Phi-tiny-MoE-instruct fully quantized. Checkpoint produced at output/Phi-tiny-MoE-instruct/.
  </done>
</task>

<task type="auto">
  <name>Task 3: Patch trust_remote_code=True into eval and reconstruct pipelines</name>
  <files>scripts/eval_ppl.py, scripts/reconstruct_model.py, scripts/run_eval.py</files>
  <action>
SlimMoE models (including Phi-tiny-MoE-instruct) use custom modeling code and require `trust_remote_code=True` in all `from_pretrained` calls. Without this, model loading fails.

1. Read `scripts/eval_ppl.py` and find every `AutoModelForCausalLM.from_pretrained` and `AutoTokenizer.from_pretrained` call. Add `trust_remote_code=True` to each if not already present.

2. Read `scripts/reconstruct_model.py` and do the same — add `trust_remote_code=True` to all `from_pretrained` calls.

3. Read `scripts/run_eval.py` and do the same — add `trust_remote_code=True` to all `from_pretrained` calls.

4. Also check `nanoquant/reconstruct.py` for any `from_pretrained` calls and patch those too.

Note: `trust_remote_code=True` is safe here because we only run on known HuggingFace models (microsoft/Phi-tiny-MoE-instruct). The flag is required because SlimMoE defines custom PhiMoE* classes in the repo.
  </action>
  <verify>
Run: `grep -n "trust_remote_code" scripts/eval_ppl.py scripts/reconstruct_model.py scripts/run_eval.py` — every `from_pretrained` call should have `trust_remote_code=True`.
  </verify>
  <done>
All from_pretrained calls in eval_ppl.py, reconstruct_model.py, and run_eval.py pass trust_remote_code=True. SlimMoE models will load correctly.
  </done>
</task>

<task type="auto">
  <name>Task 4: Measure WikiText-2 perplexity (FP16 baseline + quantized)</name>
  <files>results/Phi-tiny-MoE-instruct/metrics.json, results/SUMMARY.md</files>
  <action>
Run the evaluation pipeline:

```bash
python scripts/run_eval.py \
  --model microsoft/Phi-tiny-MoE-instruct \
  --checkpoint output/Phi-tiny-MoE-instruct \
  --output results/Phi-tiny-MoE-instruct
```

This should:
1. Measure FP16 baseline WikiText-2 perplexity
2. Reconstruct quantized model from checkpoint
3. Measure quantized WikiText-2 perplexity
4. Report memory footprint (model size before/after, peak VRAM)
5. Save results to `results/Phi-tiny-MoE-instruct/metrics.json`

After eval completes, update `results/SUMMARY.md` to include the Phi-tiny-MoE-instruct row in the results table.

If `run_eval.py` doesn't support all these steps automatically, run the individual eval scripts as needed (eval_ppl.py for baseline, reconstruct_model.py + eval_ppl.py for quantized).
  </action>
  <verify>
`results/Phi-tiny-MoE-instruct/metrics.json` exists and contains fp16_ppl, quantized_ppl, and memory metrics. `results/SUMMARY.md` includes Phi-tiny-MoE row.
  </verify>
  <done>
WikiText-2 perplexity measured for both FP16 baseline and quantized Phi-tiny-MoE-instruct. Results saved to metrics.json and SUMMARY.md updated.
  </done>
</task>

</tasks>

<verification>
- `ls output/Phi-tiny-MoE-instruct/` shows checkpoint files
- `cat results/Phi-tiny-MoE-instruct/metrics.json` shows PPL numbers for both baseline and quantized
- `grep "Phi-tiny-MoE" results/SUMMARY.md` shows results in summary table
</verification>

<success_criteria>
- Phi-tiny-MoE-instruct quantizes end-to-end on 12GB GPU without OOM or crash
- Checkpoint is produced and loadable
- FP16 baseline WikiText-2 PPL is measured
- Quantized WikiText-2 PPL is measured
- Memory footprint reported
- Results stored in metrics.json and SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/04-phi-moe-support/04-02-SUMMARY.md`
</output>
