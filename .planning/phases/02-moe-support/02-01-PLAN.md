---
phase: 02-moe-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nanoquant/moe.py
autonomous: true
requirements:
  - MOE-01
  - MOE-02
  - MOE-04

must_haves:
  truths:
    - "get_weight_views yields WeightViews for OlmoeExperts modules"
    - "get_weight_views yields WeightViews for Qwen3MoeExperts and Qwen3_5MoeExperts modules"
    - "get_hessian_key returns correct shared Hessian key for all fused expert class types"
    - "nn.Linear ancestor check excludes all fused expert classes, not just Qwen2MoeExperts"
    - "Expert views within a block are yielded contiguously (all experts before next module)"
  artifacts:
    - path: "nanoquant/moe.py"
      provides: "FUSED_EXPERT_CLASSES dispatch set + updated get_weight_views and ancestor check"
      contains: "FUSED_EXPERT_CLASSES"
  key_links:
    - from: "nanoquant/moe.py"
      to: "nanoquant/reconstruct.py"
      via: "get_weight_views import"
      pattern: "from nanoquant.moe import get_weight_views"
---

<objective>
Extend WeightView and Hessian key logic to handle all four MoE expert class types via a dispatch table.

Purpose: The existing moe.py hardcodes "Qwen2MoeExperts" string matching. This plan replaces it with a FUSED_EXPERT_CLASSES frozenset covering OlmoeExperts, Qwen3MoeExperts, and Qwen3_5MoeExperts — all using the identical fused 3D tensor pattern (gate_up_proj, down_proj). This satisfies MOE-01 (OLMoE), MOE-02 (Qwen variants), and ensures MOE-04 (expert grouping) continues to work since experts are yielded contiguously by get_weight_views.

Output: Updated nanoquant/moe.py with multi-architecture support.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-moe-support/02-RESEARCH.md
@nanoquant/moe.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded Qwen2MoeExperts with FUSED_EXPERT_CLASSES dispatch table</name>
  <files>nanoquant/moe.py</files>
  <action>
1. Add a module-level frozenset at the top of moe.py (after imports):

```python
# All known fused-expert MoE module classes.
# Each stores gate_up_proj and down_proj as 3D nn.Parameter [n_experts, out, in].
# Source: HuggingFace transformers modeling files (verified 2026-02-24).
FUSED_EXPERT_CLASSES = frozenset({
    "Qwen2MoeExperts",    # Qwen1.5-MoE, Qwen2-MoE  (model_type: qwen2_moe)
    "Qwen3MoeExperts",    # Qwen3-MoE                (model_type: qwen3_moe)
    "Qwen3_5MoeExperts",  # Qwen3.5-MoE              (model_type: qwen3_5_moe)
    "OlmoeExperts",       # OLMoE-1B-7B              (model_type: olmoe)
})
```

2. In `get_weight_views`, replace `if type_name == "Qwen2MoeExperts":` with `if type_name in FUSED_EXPERT_CLASSES:`. The body stays identical — all four classes use the same gate_up_proj/down_proj 3D parameter layout.

3. In the nn.Linear ancestor check (the `is_inside_experts` block around lines 118-131), replace `if type(ancestor).__name__ == "Qwen2MoeExperts":` with `if type(ancestor).__name__ in FUSED_EXPERT_CLASSES:`. This prevents double-yielding of nn.Linear children inside ANY fused expert module.

4. Update the docstring of `get_weight_views` to list all four expert class types instead of just "Qwen2MoeExperts".

5. Update the docstring of `get_hessian_key` to say "fused expert modules" instead of specifically "Qwen2MoeExperts". No logic change needed in get_hessian_key — the `[` bracket check and `.gate_up_proj` / `.down_proj` string parsing is already generic and works for all fused expert classes.

Do NOT change any logic in how weights are sliced, written back, or how Hessian keys are computed. Only the type-name matching strings change.
  </action>
  <verify>
Run: `python -c "from nanoquant.moe import FUSED_EXPERT_CLASSES, get_weight_views; print(FUSED_EXPERT_CLASSES); print('OK')"` — should print the frozenset and "OK" with no import errors.

Verify the frozenset contains exactly 4 entries: OlmoeExperts, Qwen2MoeExperts, Qwen3MoeExperts, Qwen3_5MoeExperts.
  </verify>
  <done>
FUSED_EXPERT_CLASSES frozenset exists with 4 class names. get_weight_views uses `in FUSED_EXPERT_CLASSES` instead of `== "Qwen2MoeExperts"`. Ancestor check uses the same set. Docstrings updated. No functional logic changed.
  </done>
</task>

</tasks>

<verification>
- `python -c "from nanoquant.moe import FUSED_EXPERT_CLASSES; assert len(FUSED_EXPERT_CLASSES) == 4"` passes
- `python -c "from nanoquant.moe import get_weight_views; print('import OK')"` passes
- `grep -c "Qwen2MoeExperts" nanoquant/moe.py` returns exactly 1 (only in the FUSED_EXPERT_CLASSES set, not in logic branches)
</verification>

<success_criteria>
- moe.py dispatches on FUSED_EXPERT_CLASSES set (not hardcoded string)
- All 4 expert class names are in the set
- No hardcoded "Qwen2MoeExperts" remains in conditional branches
- Existing Qwen2MoeExperts behavior unchanged (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/02-moe-support/02-01-SUMMARY.md`
</output>
