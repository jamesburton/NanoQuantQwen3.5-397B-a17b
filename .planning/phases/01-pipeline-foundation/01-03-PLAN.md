---
phase: 01-pipeline-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - nanoquant/hardware.py
  - requirements.txt
autonomous: true
gap_closure: true
requirements:
  - PIPE-01

must_haves:
  truths:
    - "print_hardware_summary() displays device=cuda when GPU is usable"
    - "print_hardware_summary() distinguishes model loading (CPU) from block computation (cuda)"
    - "HardwarePlan includes dstorage_available field"
    - "dstorage-gpu is detected on Windows when installed"
  artifacts:
    - path: "nanoquant/hardware.py"
      provides: "Updated HardwarePlan with dstorage_available + clearer summary output"
      contains: "dstorage_available"
    - path: "requirements.txt"
      provides: "dstorage-gpu optional dependency"
      contains: "dstorage-gpu"
  key_links:
    - from: "nanoquant/hardware.py"
      to: "print_hardware_summary output"
      via: "plan.device field included in print"
      pattern: "device=.*plan\\.device"
---

<objective>
Fix hardware summary display to show device=cuda when GPU is usable, clarify model-load vs computation device, and add dstorage-gpu detection for Windows DirectStorage.

Purpose: UAT test 1 reported summary shows strategy=cpu even with 10GB GPU — confusing because device (cuda) is omitted. User also requested dstorage-gpu support.
Output: Updated hardware.py and requirements.txt
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@nanoquant/hardware.py
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hardware summary and add dstorage-gpu detection</name>
  <files>nanoquant/hardware.py, requirements.txt</files>
  <action>
In nanoquant/hardware.py:

1. Add `dstorage_available: bool` field to HardwarePlan dataclass (after `device`).

2. In `probe_hardware()`, after the existing device assignment, add dstorage-gpu detection:
   ```python
   dstorage_available = False
   if sys.platform == "win32" and cuda_available:
       try:
           import dstorage  # dstorage-gpu package
           dstorage_available = True
       except ImportError:
           pass
   ```
   Add `import sys` at top of file. Pass `dstorage_available=dstorage_available` to HardwarePlan constructor.

3. Rewrite `print_hardware_summary()` to show BOTH device and model_load_strategy clearly. New format:
   ```
   Hardware: GPU=10.2GB usable | CPU=28.4GB | disk=145.3GB | compute=cuda, model_load=cpu | dstorage=yes
   ```
   - Show `compute={plan.device}` (this is what the user cares about — where blocks run)
   - Show `model_load={plan.model_load_strategy}` (always cpu by design)
   - Show `dstorage=yes/no` only on Windows (check `sys.platform == "win32"`)
   - Update the docstring example to match new format

In requirements.txt:
4. Add line: `dstorage-gpu; sys_platform == "win32"` after the psutil line. This is a PEP 508 environment marker so it only installs on Windows.
  </action>
  <verify>
Run: `python -c "from nanoquant.hardware import HardwarePlan; print([f.name for f in HardwarePlan.__dataclass_fields__.values()])"` — must include "dstorage_available".
Run: `python -c "from nanoquant.hardware import probe_hardware, print_hardware_summary; p = probe_hardware(); print_hardware_summary(p)"` — output must contain "compute=" and "model_load=".
Check requirements.txt contains dstorage-gpu with win32 marker.
  </verify>
  <done>print_hardware_summary() shows compute=cuda (or cpu) and model_load=cpu distinctly. HardwarePlan has dstorage_available field. dstorage-gpu is in requirements.txt with Windows guard.</done>
</task>

</tasks>

<verification>
- `python -c "from nanoquant.hardware import probe_hardware, print_hardware_summary; p = probe_hardware(); print_hardware_summary(p)"` shows compute= and model_load= fields
- HardwarePlan dataclass has dstorage_available field
- requirements.txt includes dstorage-gpu with platform marker
</verification>

<success_criteria>
UAT test 1 gap is closed: summary clearly shows GPU computation device separate from model loading strategy, and dstorage-gpu is detected on Windows.
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-foundation/01-03-SUMMARY.md`
</output>
