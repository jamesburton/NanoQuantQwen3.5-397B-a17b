---
phase: 03-scaling-and-evaluation
plan: 02b
task: 2
total_tasks: 4
status: in_progress
last_updated: 2026-03-16T12:00:00.000Z
---

<current_state>
Fresh OLMoE-1B-7B-0924 quantization is running in background (bpw=0.80, rank=819).
This is the third attempt, now with the Hessian-input mismatch bug fixed.

Background task ID: b1d2g01bi
Log file: logs/olmoe-03-02b-hessian-fix.log

Command running:
```
python scripts/run_stage1.py \
  --model allenai/OLMoE-1B-7B-0924 \
  --output output/OLMoE-1B-7B-0924 \
  --bits-per-weight 0.80 \
  --admm-iters 50 \
  --n-calibration 128 \
  --seq-len 2048 \
  --checkpoint-dir checkpoints/OLMoE-1B-7B-0924
```
</current_state>

<completed_work>

- Task 1: Diagnose block 11 instability — DONE
  - Root cause 1: rank=819 (bpw=0.80) too aggressive → non-finite block output → clamped
  - Clamping produced garbage weights for block 11 AND blocks 12-15 (corrupted calibration)
  - Root cause 2 (deeper): Hessian-input mismatch in reconstruct.py
    - Pre-captured Hessians used original model activations
    - ADMM reconstruction uses quantized-previous-block outputs
    - Mismatch → ADMM finds degenerate solutions with spike weights (max 48 vs normal 0.025)
    - Result: activation explosion starting block 2 (7.8x), reaching 314x at block 10
    - Final PPL: 21,425 vs baseline 6.62 (3,236x degradation — threshold is 20x)

- Task 2: Resume quantization blocks 11–15 — SUPERSEDED
  - Earlier attempts with bpw=0.55 + clamp fix completed 16 blocks but PPL was NaN (float16 CPU)
  - After fixing float16 eval issue: PPL was 21,425 due to Hessian-input mismatch
  - Fix applied: reconstruct.py now computes d_in from layer_inputs_map (actual layer inputs)
    instead of pre-captured Hessians
  - Commit: fix(reconstruct): use actual layer inputs for Hessian preconditioning (18b8ace)

- Additional fixes applied:
  - nanoquant/quantize.py: clamp non-finite block output instead of raise (preserves partial work)
  - scripts/recover_checkpoint.py: rebuild factors from block checkpoints (handles disk-full failures)
  - run_eval.py: identified float16-on-CPU NaN issue (eval must use float32 on CPU or CUDA)

</completed_work>

<remaining_work>

- Task 2 (current): Wait for fresh quantization with fixed Hessian to complete all 16 blocks
  - Check: `ls checkpoints/OLMoE-1B-7B-0924/ | wc -l` → should reach 16
  - Expected behavior: no activation explosion, blocks should reconstruct cleanly
  - If block 11 non-finite again: already handled by clamp (won't abort, but check quality)

- Task 3: Run evaluation and update metrics
  ```bash
  python scripts/run_eval.py \
    --model allenai/OLMoE-1B-7B-0924 \
    --quantized-dir output/OLMoE-1B-7B-0924 \
    --results-dir results \
    --stride 512 \
    --max-length 2048
  ```
  Target: degradation_ratio <= 20x (FP16 ~6.62 → quantized <= ~132)

- Task 4: Checkpoint — human verify PPL is usable before proceeding to 03-02c

After 03-02b completes, next plan is 03-02c: decide Qwen1.5-MoE-A2.7B approach.
After 03-02c, proceed to 03-03 (cloud GPU: Qwen2-57B + Qwen3.5-397B).

</remaining_work>

<decisions_made>

- bpw=0.80 (rank=819) kept for the fixed run — the Hessian mismatch was the root cause of
  poor quality, not the rank being too aggressive
- Block output clamp preserved (safer than raise — allows recovery from deep-block instability)
- Sequential Hessian fix: free (zero extra compute, uses already-captured layer_inputs_map)
- If PPL still fails at bpw=0.80 after Hessian fix: escalate to higher admm-iters=200
- Phase 3 restructured: added 03-02b (OLMoE fix) and 03-02c (Qwen1.5 decision) as gates before 03-03
- PPL usability threshold: 20x degradation (FP16 baseline ~6.62 → quantized <= ~132)
- 03-03 (cloud GPU spend) blocked until OLMoE proves pipeline end-to-end

</decisions_made>

<blockers>

- Fresh quantization duration: ~1h Hessian capture + ~30-60min per block × 16 blocks on CPU
- Disk space: 92GB free, need ~27GB checkpoints + ~28GB factors = 55GB total (OK)
- If Hessian fix insufficient: try --admm-iters 200 next

</blockers>

<next_action>
1. Wait for quantization to complete: `ls checkpoints/OLMoE-1B-7B-0924/ | wc -l` (want 16)
   - Monitor: tail logs/olmoe-03-02b-hessian-fix.log
   - If completed with 16 blocks: run evaluation (Task 3 command above)
   - If block 11 clamped again: OK — check quality, don't abort
   - If still fails: try --admm-iters 200

2. Run evaluation (Task 3)

3. Check activation magnitudes per block to verify no explosion
   (should see all blocks within ~2x of baseline if fix worked)

4. Update metrics.json, SUMMARY.md, commit

5. Human checkpoint (Task 4)
</next_action>
