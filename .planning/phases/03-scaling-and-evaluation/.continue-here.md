---
phase: 03-scaling-and-evaluation
plan: 02b
task: 2
total_tasks: 4
status: in_progress
last_updated: 2026-03-15T19:11:25.847Z
---

<current_state>
OLMoE-1B-7B-0924 quantization (blocks 11–15) is actively running in background.

The Python process was restarted after the executor agent session was interrupted by a rate limit. It is re-capturing Hessians (~1h on CPU) then will reconstruct blocks 11–15.

Background task ID: bwojhdn83
Log file: logs/olmoe-03-02b-resume.log

Command running:
```
python scripts/run_stage1.py \
  --model allenai/OLMoE-1B-7B-0924 \
  --output output/OLMoE-1B-7B-0924 \
  --bits-per-weight 0.55 \
  --admm-iters 50 \
  --n-calibration 128 \
  --seq-len 2048 \
  --checkpoint-dir checkpoints/OLMoE-1B-7B-0924
```
</current_state>

<completed_work>

- Task 1: Diagnose block 11 instability — DONE
  - Root cause: rank=819 (bpw=0.80) too aggressive for deeper OLMoE blocks → non-finite block output
  - Fix: reduce to bpw=0.55 → rank=563
  - Confirmed 11 block checkpoints present (blocks 0–10)

- Task 2: Resume quantization blocks 11–15 — IN PROGRESS
  - First attempt: killed when executor agent hit rate limit (session ended mid-Hessian-capture)
  - Second attempt: relaunched manually as background task bwojhdn83
  - Hessian capture confirmed working (completed successfully in first attempt, ~1h 2min for 128 samples)
  - Block reconstruction starting at block 11 — `Blocks: 0%| 0/5` was the last observed state
  - Current checkpoint count: still 11/16 (blocks 0–10 only)

</completed_work>

<remaining_work>

- Task 2 (continuing): Wait for quantization process to complete all 5 remaining blocks (11–15)
  - Check: `ls checkpoints/OLMoE-1B-7B-0924/ | wc -l` → should reach 16
  - Check: `output/OLMoE-1B-7B-0924/quantized_factors.safetensors` exists with all 16 blocks

- Task 3: Run evaluation and update metrics
  ```bash
  python scripts/run_eval.py \
    --model allenai/OLMoE-1B-7B-0924 \
    --quantized-dir output/OLMoE-1B-7B-0924 \
    --results-dir results \
    --stride 512 \
    --max-length 2048
  ```
  Then update results/OLMoE-1B-7B-0924/metrics.json:
  - blocks_quantized: 16, partial_quantization: false, failure_reason: null
  - loadable_checkpoint: true (if PPL is reasonable)
  Update results/SUMMARY.md with complete row.
  **Usable threshold: degradation_ratio <= 20x** (FP16 ~6.62 → quantized <= ~132)

- Task 4: Checkpoint — human verify PPL is usable before proceeding to 03-02c

After 03-02b completes, next plan is 03-02c: decide Qwen1.5-MoE-A2.7B approach (Option A/B/C).
After 03-02c, proceed to 03-03 (cloud GPU: Qwen2-57B + Qwen3.5-397B).

</remaining_work>

<decisions_made>

- bpw=0.55 (rank=563) chosen over bpw=0.80 (rank=819) to fix ADMM instability at depth
- Phase 3 restructured: added 03-02b (OLMoE fix) and 03-02c (Qwen1.5 decision) as gates before 03-03
- Qwen1.5-MoE-A2.7B split into its own plan (03-02c) — 26.67 GB model, eval infeasible locally
- PPL usability threshold set at 20x degradation (FP16 baseline ~6.62 → quantized <= ~132)
- 03-03 (cloud GPU spend) blocked until OLMoE proves pipeline end-to-end

</decisions_made>

<blockers>

- Background quantization process duration unknown — each block at bpw=0.55 may still take 30–90 min on CPU
- If block 11 still produces non-finite output with bpw=0.55: escalate to also add --rho 0.1, or investigate making the block-output check recoverable (clamp instead of raise in nanoquant/quantize.py:283)
- CUDA was unavailable in previous session (driver bad state after force-killing Qwen eval); process was restarted in CPU-only mode (Hardware: GPU=0.0GB — confirmed in log)

</blockers>

<context>
The core problem is validating the pipeline end-to-end before spending cloud GPU money. OLMoE-1B-7B-0924 is the smallest available MoE model and our test bed. The previous run got 11/16 blocks but hit numerical instability; reducing bpw is the standard fix (lower rank = less aggressive quantization = more stable ADMM at deeper layers).

The Qwen1.5 split was a user decision: don't burn cloud time until we have proof OLMoE works completely. 03-02c will decide whether Qwen1.5 needs full quantization+cloud eval or if OLMoE alone is sufficient local validation.

bpw=0.55 gives rank=563 for OLMoE's hidden_dim=2048. The previous bpw=0.80 gave rank=819 which was ~80% of d/2 — very aggressive. 0.55 → rank=563 is ~55% which should be more stable.
</context>

<next_action>
1. Check if quantization completed: `ls checkpoints/OLMoE-1B-7B-0924/ | wc -l` (want 16)
   - If still running: wait and check the log at logs/olmoe-03-02b-resume.log
   - If completed with 16 blocks: proceed to Task 3 (run eval)
   - If failed again at block 11: try with --rho 0.1 added

2. Once 16 blocks confirmed, run evaluation (Task 3 command above)

3. Update metrics.json and SUMMARY.md, commit

4. Human checkpoint (Task 4) — verify PPL <= 20x degradation before 03-02c
</next_action>
