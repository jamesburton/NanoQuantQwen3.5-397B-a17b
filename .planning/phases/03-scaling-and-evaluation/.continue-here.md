---
phase: 03-scaling-and-evaluation
plan: 02b
task: 4
total_tasks: 4
status: at_checkpoint
last_updated: 2026-03-16T18:00:00.000Z
---

<current_state>
03-02b is at Task 4 (human checkpoint). Tasks 1-3 complete.

Pipeline smoke test: PASSED — end-to-end flow works (Hessian capture → ADMM → checkpoint → reconstruct → eval).
Quality test: FAILED — bpw=0.80 with 50 ADMM iterations produces unusable PPL on OLMoE-1B-7B-0924.

Awaiting human decision on next steps (see next_action below).
</current_state>

<completed_work>

- Task 1: Diagnose block 11 instability — DONE
  - Root cause 1: rank=819 (bpw=0.80) too aggressive → non-finite block output → clamped
  - Clamping produced garbage weights for block 11 AND blocks 12-15 (corrupted calibration)
  - Root cause 2 (deeper): Hessian-input mismatch in reconstruct.py
    - Pre-captured Hessians used original model activations
    - ADMM reconstruction uses quantized-previous-block outputs
    - Mismatch → ADMM finds degenerate solutions with spike weights (max 48 vs normal 0.025)
    - Result: activation explosion starting block 2 (7.8x), reaching 314x at block 10
    - Final PPL: 21,425 vs baseline 6.62 (3,236x degradation — threshold is 20x)

- Task 2: Quantization — DONE (quality unusable)
  - Two Hessian approaches tested:
    a) Original pre-captured Hessians: activation EXPLOSION starting at block 2 (7.8x → 314x by block 10).
       PPL with blocks 0-10 quantized + 11-15 FP16: 21,425 (3,236x degradation)
    b) Actual-layer-input Hessians (fix attempted and reverted): activation COLLAPSE (50x below baseline
       for blocks 1-11). PPL all 16 blocks: 881,014
  - Root cause: binary factorization at bpw=0.80 with 50 ADMM iterations doesn't have enough
    approximation capacity for OLMoE's weight matrices. Error compounds multiplicatively through 16 blocks.
  - Block-by-block activation analysis confirmed progressive error — even blocks 0-1 have small errors
    that compound dramatically through subsequent blocks.
  - Block 11 specifically had non-finite output with earlier bpw=0.55 run (clamped, corrupting blocks 12-15 calibration data)

- Task 3: Evaluation — DONE
  - FP16 baseline PPL: 6.6181 (confirmed correct)
  - Quantized PPL: unusable (21,425 for partial quantization, 881,014 for full)
  - Float16 on CPU produces NaN (float16 matmuls overflow on CPU) — eval must use float32 on CPU or float16 on CUDA
  - The NaN PPL from earlier sessions was caused by float16-on-CPU, not model corruption

- Bug fixes committed:
  - nanoquant/quantize.py: clamp non-finite block output instead of raise (preserves partial work)
  - scripts/recover_checkpoint.py: new script to rebuild factors from block checkpoints
  - Hessian fix attempted and reverted (made things worse — activation collapse instead of explosion)

</completed_work>

<remaining_work>

- Task 4 (current): Human checkpoint — decide next steps

After 03-02b completes, next plan is 03-02c: decide Qwen1.5-MoE-A2.7B approach.
After 03-02c, proceed to 03-03 (cloud GPU: Qwen2-57B + Qwen3.5-397B).

</remaining_work>

<decisions_made>

- bpw=0.80 at OLMoE scale is NOT viable with current ADMM settings (50 iterations)
- Block output clamp preserved (safer than raise — allows recovery from deep-block instability)
- Hessian fix (actual layer inputs) reverted — caused activation collapse instead of explosion, not an improvement
- Phase 3 restructured: added 03-02b (OLMoE fix) and 03-02c (Qwen1.5 decision) as gates before 03-03
- PPL usability threshold: 20x degradation (FP16 baseline ~6.62 → quantized <= ~132)
- Pipeline code is validated — no blocking bugs for cloud GPU runs
- 03-02b conclusion: pipeline smoke test PASSED, quality test FAILED

</decisions_made>

<blockers>

- Sub-1-bit compression at bpw=0.80 may need:
  (a) Larger models with more redundancy (bigger models tolerate more compression)
  (b) Higher bpw (2.0-4.0 range)
  (c) Significantly more ADMM iterations (200+)

</blockers>

<next_action>
Task 4: Human decision required. Three options:

Option A: Proceed to cloud GPU runs (03-03)
  - Pipeline is validated end-to-end, code works
  - Larger models (Qwen2-57B, Qwen3.5-397B) may compress better due to more redundancy
  - Risk: same quality issue at larger scale, but cloud GPU spend is justified by pipeline validation

Option B: Test higher bpw locally (2.0-4.0)
  - Stay on OLMoE-1B-7B-0924 but increase bits-per-weight
  - Would confirm whether bpw is the primary quality lever
  - Lower risk, lower cost, but delays cloud runs

Option C: More ADMM iterations locally (200+)
  - Keep bpw=0.80 but give ADMM more time to converge
  - ~4x longer runtime per block
  - May not help if rank=819 fundamentally lacks capacity

Reply with A, B, or C (or a combination) to proceed.
</next_action>
